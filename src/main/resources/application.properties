spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/banking_simulator?createDatabaseIfNotExist=true}
spring.datasource.username=root
spring.datasource.password=52572622

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

server.port=${SERVER_PORT:8082}
auth.database.name=${AUTH_DB_NAME:bank_auth}
app.jwt.secret=${JWT_SECRET:inbankSmartBankingJwtSecretKeyForProduction1234567890}
app.jwt.expiration-minutes=${JWT_EXPIRY_MINUTES:120}

spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:1025}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:false}
bank.mail.from=${BANK_MAIL_FROM:no-reply@inbank.local}

bank.alert.low-balance-threshold=${BANK_LOW_BALANCE_THRESHOLD:1000}
bank.alert.large-transaction-threshold=${BANK_LARGE_TRANSACTION_THRESHOLD:10000}